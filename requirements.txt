# Python 3.11 — matches apache/airflow:2.8.1-python3.11 in docker-compose.yaml
# Local dev: python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

apache-airflow==2.8.1
great-expectations==1.18.1
soda-core==3.5.6
soda-core-duckdb==3.5.6
boto3>=1.34,<2.0
pandas>=2.0,<3.0
duckdb>=1.0,<2.0
setuptools>=70.0
